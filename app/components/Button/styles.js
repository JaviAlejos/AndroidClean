import { StyleSheet } from 'react-native';

export default styles = StyleSheet.create({
    button: {
        flex:1,
        alignItems: 'center',
        justifyContent: 'center',
        //borderRadius: 8,
        //borderWidth: 8,
        //borderColor: '#ffffff', 
    }
});